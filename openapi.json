{
  "openapi": "3.0.1",
  "info": {
    "title": "结构关键词检测系统插件",
    "version": "1.0.0",
    "description": "基于语义方式构建和识别结构的结构系统，用于理解结构关键词和回复结构反应"
  },
  "servers": [
    {
       "url": "https://structure-api-render.onrender.com"

    }
  ],
  "paths": {
    "/predict": {
      "post": {
        "summary": "结构关键词检测接口",
        "operationId": "structure_predict",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "user_input": {
                    "type": "string",
                    "description": "待分析的结构关键词或语句"
                  }
                },
                "required": ["user_input"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "结构分析的关键词检测返回值",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "response": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
